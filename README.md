# Diffusion Model Image Classifier

## 🎯 Цель проекта

### Проблема
С развитием генеративных AI-моделей, особенно диффузионных моделей, резко возросло количество синтетически сгенерированных изображений. Это создает несколько серьезных вызовов:

1. **Распространение фейкового контента**: От реалистичных фотографий несуществующих событий до коммерческих изображений несуществующих товаров
2. **Проблемы доверия**: Пользователи не могут отличить реальные изображения от сгенерированных
3. **Контент-модерация**: Платформы сталкиваются с трудностями в автоматической фильтрации синтетического контента

### Научно-техническая цель
Разработка высокоточного классификатора, способного определить конкретную диффузионную модель, использованную для генерации изображения. В отличие от бинарной классификации "реальное vs сгенерированное", наша модель решает более сложную задачу - идентификацию источника генерации среди 25 различных диффузионных моделей.

### Бизнес-цели

#### 🛡️ Борьба с фейковым контентом
**Прямое применение:**
- **Верификация контента** для новостных агентств и социальных сетей
- **Автоматическая модерация** на платформах пользовательского контента

**Бизнес-ценность:**
- Снижение репутационных рисков для платформ
- Уменьшение затрат на ручную модерацию контента
- Предотвращение распространения дезинформации

#### 🏷️ Оценка качества контента
**Прямое применение:**
- **Индикатор качества товаров** в e-commerce - изображения, сгенерированные более современными моделями, часто соответствуют товарам более высокого качества
- **Фильтрация контента** по уровню "профессионализма" генерации
- **Анализ трендов** в использовании генеративных моделей

**Бизнес-ценность:**
- Повышение доверия покупателей к платформам
- Улучшение пользовательского опыта за счет фильтрации низкокачественного контента
- Data-driven insights для маркетинговых стратегий

#### 💰 Оценка ресурсов генерации
**Прямое применение:**
- **Косвенная оценка стоимости** создания контента
- **Выявление автоматизированного контента** в маркетинговых кампаниях
- **Анализ конкурентов** - понимание технологического стека, используемого другими компаниями

### Долгосрочная перспектива
Данный проект служит фундаментом для более сложных систем:
- **Бинарный классификатор** "реальное vs сгенерированное"
- **Детектирование гибридного контента** (частично сгенерированного)
- **Оценка временной метки** генерации (когда было создано изображение)
- **Прогнозирование трендов** в развитии генеративных моделей

## 📊 Набор данных

### Обзор датасета DRAGON
Используется датасет [DRAGON (Diffusion-model geneRated imAge classification Benchmark)](https://huggingface.co/datasets/lesc-unifi/dragon) - специализированный бенчмарк для классификации изображений по модели-источнику.

### Характеристики датасета
- **25 классов** - различных диффузионных моделей
- **Сбалансированные классы** - равномерное распределение по моделям
- **Высокое качество** - изображения сгенерированы с стандартными параметрами качества
- **Разнообразие контента** - различные типы изображений (объекты, сцены, арты)

### Модели в датасете
Датасет включает как известные публичные модели, так и специализированные:
- Stable Diffusion (различные версии)
- Flux_1
- Kandinsky
- И другие современные диффузионные модели

## 🏗️ Архитектура решения

### Модельный стек
- **Базовая модель**: ResNet50 с transfer learning
- **Размер изображений**: 224x224 пикселей
- **Предобработка**: Normalization (ImageNet statistics)
- **Аугментации**: RandomHorizontalFlip
- **Фреймворк**: PyTorch + HuggingFace Transformers

### Конвейер обработки
1. **Загрузка данных** - потоковая загрузка из HuggingFace Hub
2. **Предобработка** - ресайз, нормализация, аугментация
3. **Обучение** - fine-tuning предобученной модели
4. **Валидация** - оценка на валидационной выборке
5. **Сохранение** - публикация в HuggingFace Hub

### Ключевые особенности
- **Воспроизводимость** - фиксированные random seeds
- **Масштабируемость** - модульная архитектура
- **Интеграция** - совместимость с HuggingFace ecosystem

## 🎯 Целевые метрики

### Метрики качества модели
| Метрика | Целевое значение | Обоснование |
|---------|------------------|-------------|
| **Accuracy** | ≥ 90% | Учитывая 25 классов, случайный угадывание = 4% |




# Запуск
```
conda env create -f requirements.yml
```

Тестирование корректности установки:
```
conda activate diffclass
python testing_code.py
```

Запуск обучение:
```
bash run.sh
```
Рекомендуется установить API ключ внутри скрипта. Модель достигает accuracy на валидации **0.94**, чекпоинт хранится на HF [по ссылке](https://huggingface.co/3145tttt/diffusion-classification_base_resnet_50), параметры обучения находятся в [config](./configs/resnet_baseconf.yaml). Пример использования модели в файле [inference.py](./inference.py). Модель была обученна на датасете dragon размером `Regular (R)`: 100 промтов, 25000 обучающих изображений и 10090 изображений для валидации.

# Работа с DVC

Для контроля версий данных и датасета используется DVC. Для подгрузки данных необходимо после `git clone` выполнить:
```
dvc pull
```

Для воспроизведения результатов достаточно выполнить команду `dvc repro`, которая запустит тестирование, подготовку данных, обучение и тестирование. Текущий конфиг и код в `dvc.yaml` запускают код обучения на датасете dragon размером `ExtraSmall (XS)`: 10 промтов, 250 обучающих изображений и 1000 изображений для валидации.

# Логирование

Для логирования используется библиотека `comet_ml`. Логи `comet_ml` доступны по [ссылке](https://www.comet.com/enteralt/mlops/view/new/panels). 

Кроме того, дополнительно модель была обученна на датасете dragon размером `Regular (R)`: 100 промтов, 25000 обучающих изображений и 10090 изображений для валидации, логи для этого запуска, также доступны по [ссылке](https://www.comet.com/enteralt/mlops/view/new/panels). Модель обученная на датасете `Regular` показывает выше качество, поэтому чекпоинт был сохранён на [Hugging Face](https://huggingface.co/3145tttt/diffusion-classification_base_resnet_50). По умолчанию, если нет чекпоинта обученной модели для инференса используется чекпоинт с Hugging Face.

# Docker
Для удобства инференса был создан образ докер файла, для его сборки достаточно вызывать команду:

```
docker build -t ml-app:v1 .
```
Важно: потребуется 4GB+ свободной памяти.

Для инференса следует вызвать команду:
```
docker run -v$PWD:/app -it ml-app:v1 --input_path testing_imgs --output_path results.csv
```
Аргумент `--input_path` содержит путь до папки с изображениями, для которых нужно сделать предсказание (важно, чтобы в папке хранились только изображений), аргумент `--output_path` содержит путь для `csv` файла, в который будут записан результат работы.
